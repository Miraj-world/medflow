fastapi
uvicorn[standard]
python-jose[cryptography]
passlib[bcrypt]
pydantic
sqlalchemy
python-dotenv
alembic
cryptography

# AI module + disease system use existing dependencies (no extra packages required).

# PostgreSQL driver (used by Render in production, ignored locally with SQLite)
psycopg2-binary
