# Optional (SQLite is default for local development)
DATABASE_URL=sqlite:///./medflow.db

# JWT Configuration
SECRET_KEY=supersecretkey

# Encryption key for sensitive data
ENCRYPTION_KEY=change_this_key
